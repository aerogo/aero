package aero

// Page represents a composition of multiple components needed
// to render a page on the web. In Aero, a Page is usually bound
// to a folder on the file system which includes all the components.
type Page struct {
}
